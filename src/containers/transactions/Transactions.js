import React from 'react';

import Navigation from '../Navigation';

const Transactions = () => (
    <div>
        <Navigation />
        <h1>Transactions</h1>
    </div>
);

export default Transactions;
